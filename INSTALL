Requirements:
    
    PostgreSQL
    Redis
    pip
    python >= 3
        redis               ? (looks permissive / BSD)
        psycopg2            LGPL 3

Installation:

    pip install redis psycopg2

